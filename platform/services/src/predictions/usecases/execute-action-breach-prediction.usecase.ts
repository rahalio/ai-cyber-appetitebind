/**
 * ExecuteActionBreachPrediction - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteActionBreachPrediction Use Case
 *
 * DDD: Application use case for get Action.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ActionBreachPredictionInput, ActionBreachPredictionOutput } from "../dto/action.dto";
import type { ExecutionContextService, IdGeneratorService } from "@appetitebind/services/_shared/index.js";
import type { ActionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteActionBreachPrediction {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly action: ActionRepository
  ) {}

  async execute(input: ActionBreachPredictionInput): Promise<ActionBreachPredictionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prdId();

    // All field names extracted from OpenAPI spec: action, rationale
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).predictionId) {
      throw new ValidationError("predictionId is required");
    }
    const predictionId = (input as any).predictionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.action.actionBreachPrediction({ predictionId: predictionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${predictionId}`);
    }

    return entity as ActionBreachPredictionOutput;
  }
}
