/**
 * ExecuteRecordRiskIdentification - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: assessments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecordRiskIdentification Use Case
 *
 * DDD: Application use case for get RiskIdentification.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecordRiskIdentificationInput, RecordRiskIdentificationOutput } from "../dto/risk-identification.dto";
import type { ExecutionContextService, IdGeneratorService } from "@appetitebind/services/_shared/index.js";
import type { RiskIdentificationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRecordRiskIdentification {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly riskIdentification: RiskIdentificationRepository
  ) {}

  async execute(input: RecordRiskIdentificationInput): Promise<RecordRiskIdentificationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.asmId();

    // All field names extracted from OpenAPI spec: description, reassessmentRequired, trigger
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).useCaseId) {
      throw new ValidationError("useCaseId is required");
    }
    const usecaseId = (input as any).useCaseId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.riskIdentification.recordRiskIdentification({ useCaseId: usecaseId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${usecaseId}`);
    }

    return entity as RecordRiskIdentificationOutput;
  }
}
