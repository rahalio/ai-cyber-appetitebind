/**
 * Breach Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import type { BreachEntity } from "@appetitebind/core/predictions/models/index.js";
import { predictionsSchemas } from "@appetitebind/core/predictions";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "prd";

export function createBreach(overrides: Partial<BreachEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    predictedBreachType: overrides.predictedBreachType ?? "appetite",
    predictionId: overrides.predictionId ?? `prd_${ulid().toLowerCase()}`,
    status: overrides.status ?? "open",
    useCaseId: overrides.useCaseId ?? `ucs_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return predictionsSchemas.BreachPrediction.parse(raw);
}

export function breachBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createBreach(overrides));
}
