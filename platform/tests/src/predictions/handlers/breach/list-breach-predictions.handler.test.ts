/**
 * ListBreachPredictions handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listBreachPredictions } from "@appetitebind/api-server/domains/predictions/handlers";
import type { PredictionsDDDDependencies } from "@appetitebind/api-server/domains/predictions";
import { createBreach as createBreachEntity } from "@appetitebind/factories/predictions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@appetitebind/tests/shared";
import { PREDICTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListBreachPredictions", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PredictionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PredictionsDDDDependencies>(
      PREDICTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createBreachEntity({ orgId }),
      createBreachEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListBreachPredictions, "execute").mockResolvedValue(useCaseResult);

    await listBreachPredictions(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListBreachPredictions.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
