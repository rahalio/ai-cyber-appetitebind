/**
 * ActionBreachPrediction handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { actionBreachPrediction } from "@appetitebind/api-server/domains/predictions/handlers";
import type { PredictionsDDDDependencies } from "@appetitebind/api-server/domains/predictions";
import { createAction as createActionEntity } from "@appetitebind/factories/predictions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@appetitebind/tests/shared";
import { PREDICTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("ActionBreachPrediction", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PredictionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PredictionsDDDDependencies>(
      PREDICTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createActionEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeActionBreachPrediction, "execute").mockResolvedValue(useCaseResult);

    await actionBreachPrediction(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeActionBreachPrediction.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
