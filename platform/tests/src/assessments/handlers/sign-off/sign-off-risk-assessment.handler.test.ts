/**
 * SignOffRiskAssessment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: assessments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { signOffRiskAssessment } from "@appetitebind/api-server/domains/assessments/handlers";
import type { AssessmentsDDDDependencies } from "@appetitebind/api-server/domains/assessments";
import { createSignOff as createSignOffEntity } from "@appetitebind/factories/assessments";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@appetitebind/tests/shared";
import { ASSESSMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("SignOffRiskAssessment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AssessmentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AssessmentsDDDDependencies>(
      ASSESSMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createSignOffEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSignOffRiskAssessment, "execute").mockResolvedValue(useCaseResult);

    await signOffRiskAssessment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSignOffRiskAssessment.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
