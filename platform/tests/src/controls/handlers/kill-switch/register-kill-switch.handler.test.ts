/**
 * RegisterKillSwitch handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: controls
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { registerKillSwitch } from "@appetitebind/api-server/domains/controls/handlers";
import type { ControlsDDDDependencies } from "@appetitebind/api-server/domains/controls";
import { createKillSwitch as createKillSwitchEntity } from "@appetitebind/factories/controls";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@appetitebind/tests/shared";
import { CONTROLS_USE_CASES } from "../__factories__/use-cases.js";

describe("RegisterKillSwitch", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ControlsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ControlsDDDDependencies>(
      CONTROLS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createKillSwitchEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeRegisterKillSwitch, "execute").mockResolvedValue(useCaseResult);

    await registerKillSwitch(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeRegisterKillSwitch.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
