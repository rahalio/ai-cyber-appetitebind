/**
 * Predictions Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const runBreachPrediction_Body = z
  .object({
    useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
    horizonDays: z.number().int().gte(1).lte(90).default(30),
  })
  .partial()
  .passthrough();
const actionBreachPrediction_Body = z
  .object({
    action: z.enum([
      'governance_task',
      'suspend_traffic',
      'reassess',
      'dismiss',
    ]),
    rationale: z.string(),
  })
  .passthrough();

export const schemas: any = {
  runBreachPrediction_Body,
  actionBreachPrediction_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/predictions/breaches',
    alias: 'listBreachPredictions',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'useCaseId',
        type: 'Query',
        schema: z
          .string()
          .regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/)
          .optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z
          .enum(['open', 'actioned', 'realised', 'dismissed'])
          .optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  predictionId: z
                    .string()
                    .regex(/^prd_[0-9A-HJKMNP-TV-Z]{26}$/),
                  useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
                  predictedBreachType: z.enum([
                    'appetite',
                    'fairness',
                    'operational_handoff',
                    'portfolio_mix',
                  ]),
                  probability: z.number().optional(),
                  predictedBy: z.string().datetime({ offset: true }).optional(),
                  leadingIndicators: z.array(z.string()).optional(),
                  status: z.enum(['open', 'actioned', 'realised', 'dismissed']),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v1/predictions/breaches/:predictionId/action',
    alias: 'actionBreachPrediction',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: actionBreachPrediction_Body,
      },
      {
        name: 'predictionId',
        type: 'Path',
        schema: z.string().regex(/^prd_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            predictionId: z.string().regex(/^prd_[0-9A-HJKMNP-TV-Z]{26}$/),
            useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
            predictedBreachType: z.enum([
              'appetite',
              'fairness',
              'operational_handoff',
              'portfolio_mix',
            ]),
            probability: z.number().optional(),
            predictedBy: z.string().datetime({ offset: true }).optional(),
            leadingIndicators: z.array(z.string()).optional(),
            status: z.enum(['open', 'actioned', 'realised', 'dismissed']),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/predictions/breaches/run',
    alias: 'runBreachPrediction',
    description: `Forecast approaching appetite or fairness threshold breaches from leading indicators before the board pack is finalised.
`,
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: runBreachPrediction_Body,
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  predictionId: z
                    .string()
                    .regex(/^prd_[0-9A-HJKMNP-TV-Z]{26}$/),
                  useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
                  predictedBreachType: z.enum([
                    'appetite',
                    'fairness',
                    'operational_handoff',
                    'portfolio_mix',
                  ]),
                  probability: z.number().optional(),
                  predictedBy: z.string().datetime({ offset: true }).optional(),
                  leadingIndicators: z.array(z.string()).optional(),
                  status: z.enum(['open', 'actioned', 'realised', 'dismissed']),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
