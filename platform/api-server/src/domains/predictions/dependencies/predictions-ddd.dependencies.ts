/**
 * Predictions DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: predictions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Predictions Domain Module - Composition root
 *
 * DDD: Composition root for predictions domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ActionRepositoryAdapter,
  BreachRepositoryAdapter,
  RunRepositoryAdapter,
} from "@appetitebind/adapters/predictions";
import { getIdGeneratorService } from "@appetitebind/adapters";
import type { AdapterDynamoDBClient } from "@appetitebind/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteActionBreachPrediction,
  ExecuteListBreachPredictions,
  ExecuteRunBreachPrediction,
} from "@appetitebind/services/predictions/usecases";
// Ports (interfaces)
import type {
  ActionRepository,
  BreachRepository,
  RunRepository,
} from "@appetitebind/services/predictions/ports";

export interface PredictionsDomainModule {
  repos: {
    actions: ActionRepository;
    breaches: BreachRepository;
    runs: RunRepository;
  };
  useCases: {
    actions: {
      get: ExecuteActionBreachPrediction;
    };
    breaches: {
      list: ExecuteListBreachPredictions;
    };
    runs: {
      create: ExecuteRunBreachPrediction;
    };
  };
}

export function buildPredictionsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PredictionsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      actions?: ActionRepository;
      breaches?: BreachRepository;
      runs?: RunRepository;
    } = {};

    obj.actions = new ActionRepositoryAdapter(dynamoClient);
    obj.breaches = new BreachRepositoryAdapter(dynamoClient);
    obj.runs = new RunRepositoryAdapter(dynamoClient);
    return {
      actions: obj.actions!,
      breaches: obj.breaches!,
      runs: obj.runs!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    actions: {
      get: new ExecuteActionBreachPrediction(executionContext, idGenerator, repos.actions),
    },
    breaches: {
      list: new ExecuteListBreachPredictions(executionContext, idGenerator, repos.breaches),
    },
    runs: {
      create: new ExecuteRunBreachPrediction(executionContext, idGenerator, repos.runs),
    },
  };
  return { repos, useCases: useCases as PredictionsDomainModule["useCases"] };
}
