/**
 * Assessments Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: assessments
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const recordRiskIdentification_Body = z
  .object({
    trigger: z.enum([
      'use_expansion',
      'data_change',
      'driver_drift',
      'regulatory_change',
      'periodic',
    ]),
    description: z.string().optional(),
    reassessmentRequired: z.boolean().optional().default(true),
  })
  .passthrough();
const createRiskAssessment_Body = z
  .object({
    useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
    technicalParameters: z.record(z.string()).optional(),
    businessParameters: z.record(z.string()).optional(),
    operationalParameters: z.record(z.string()).optional(),
  })
  .passthrough();
const recordReferenceComparison_Body = z
  .object({
    referenceType: z.enum(['non_ai_model', 'rules_engine', 'prior_version']),
    varianceSummary: z.string(),
    materialVariance: z.boolean().optional(),
    rationale: z.string().optional(),
    decisionDriversReviewed: z.array(z.string()).optional(),
  })
  .passthrough();
const signOffRiskAssessment_Body = z
  .object({
    line: z.enum(['first', 'second', 'third']),
    roleId: z.string(),
    decision: z.enum(['approve', 'conditional', 'reject']),
    comments: z.string().optional(),
    signedAt: z.string().datetime({ offset: true }).optional(),
  })
  .passthrough();

export const schemas: any = {
  recordRiskIdentification_Body,
  createRiskAssessment_Body,
  recordReferenceComparison_Body,
  signOffRiskAssessment_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/assessments',
    alias: 'listRiskAssessments',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'useCaseId',
        type: 'Query',
        schema: z
          .string()
          .regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/)
          .optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z
          .enum(['draft', 'in_review', 'signed_off', 'rejected'])
          .optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  assessmentId: z
                    .string()
                    .regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
                  useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
                  status: z.enum([
                    'draft',
                    'in_review',
                    'signed_off',
                    'rejected',
                  ]),
                  technicalParameters: z.record(z.string()).optional(),
                  businessParameters: z.record(z.string()).optional(),
                  operationalParameters: z.record(z.string()).optional(),
                  referenceComparison: z
                    .object({
                      referenceType: z.enum([
                        'non_ai_model',
                        'rules_engine',
                        'prior_version',
                      ]),
                      varianceSummary: z.string(),
                      materialVariance: z.boolean().optional(),
                      rationale: z.string().optional(),
                      decisionDriversReviewed: z.array(z.string()).optional(),
                    })
                    .passthrough()
                    .optional(),
                  signOffs: z
                    .array(
                      z
                        .object({
                          line: z.enum(['first', 'second', 'third']),
                          roleId: z.string(),
                          decision: z.enum([
                            'approve',
                            'conditional',
                            'reject',
                          ]),
                          comments: z.string().optional(),
                          signedAt: z
                            .string()
                            .datetime({ offset: true })
                            .optional(),
                        })
                        .passthrough()
                    )
                    .optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
            nextCursor: z.string().optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v1/assessments',
    alias: 'createRiskAssessment',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createRiskAssessment_Body,
      },
    ],
    response: z
      .object({
        data: z
          .object({
            assessmentId: z.string().regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
            useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
            status: z.enum(['draft', 'in_review', 'signed_off', 'rejected']),
            technicalParameters: z.record(z.string()).optional(),
            businessParameters: z.record(z.string()).optional(),
            operationalParameters: z.record(z.string()).optional(),
            referenceComparison: z
              .object({
                referenceType: z.enum([
                  'non_ai_model',
                  'rules_engine',
                  'prior_version',
                ]),
                varianceSummary: z.string(),
                materialVariance: z.boolean().optional(),
                rationale: z.string().optional(),
                decisionDriversReviewed: z.array(z.string()).optional(),
              })
              .passthrough()
              .optional(),
            signOffs: z
              .array(
                z
                  .object({
                    line: z.enum(['first', 'second', 'third']),
                    roleId: z.string(),
                    decision: z.enum(['approve', 'conditional', 'reject']),
                    comments: z.string().optional(),
                    signedAt: z.string().datetime({ offset: true }).optional(),
                  })
                  .passthrough()
              )
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v1/assessments/:assessmentId/reference-comparison',
    alias: 'recordReferenceComparison',
    description: `Compare AI outcomes to non-AI reference model or rules.`,
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: recordReferenceComparison_Body,
      },
      {
        name: 'assessmentId',
        type: 'Path',
        schema: z.string().regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            assessmentId: z.string().regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
            useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
            status: z.enum(['draft', 'in_review', 'signed_off', 'rejected']),
            technicalParameters: z.record(z.string()).optional(),
            businessParameters: z.record(z.string()).optional(),
            operationalParameters: z.record(z.string()).optional(),
            referenceComparison: z
              .object({
                referenceType: z.enum([
                  'non_ai_model',
                  'rules_engine',
                  'prior_version',
                ]),
                varianceSummary: z.string(),
                materialVariance: z.boolean().optional(),
                rationale: z.string().optional(),
                decisionDriversReviewed: z.array(z.string()).optional(),
              })
              .passthrough()
              .optional(),
            signOffs: z
              .array(
                z
                  .object({
                    line: z.enum(['first', 'second', 'third']),
                    roleId: z.string(),
                    decision: z.enum(['approve', 'conditional', 'reject']),
                    comments: z.string().optional(),
                    signedAt: z.string().datetime({ offset: true }).optional(),
                  })
                  .passthrough()
              )
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/assessments/:assessmentId/sign-off',
    alias: 'signOffRiskAssessment',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: signOffRiskAssessment_Body,
      },
      {
        name: 'assessmentId',
        type: 'Path',
        schema: z.string().regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            assessmentId: z.string().regex(/^asm_[0-9A-HJKMNP-TV-Z]{26}$/),
            useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
            status: z.enum(['draft', 'in_review', 'signed_off', 'rejected']),
            technicalParameters: z.record(z.string()).optional(),
            businessParameters: z.record(z.string()).optional(),
            operationalParameters: z.record(z.string()).optional(),
            referenceComparison: z
              .object({
                referenceType: z.enum([
                  'non_ai_model',
                  'rules_engine',
                  'prior_version',
                ]),
                varianceSummary: z.string(),
                materialVariance: z.boolean().optional(),
                rationale: z.string().optional(),
                decisionDriversReviewed: z.array(z.string()).optional(),
              })
              .passthrough()
              .optional(),
            signOffs: z
              .array(
                z
                  .object({
                    line: z.enum(['first', 'second', 'third']),
                    roleId: z.string(),
                    decision: z.enum(['approve', 'conditional', 'reject']),
                    comments: z.string().optional(),
                    signedAt: z.string().datetime({ offset: true }).optional(),
                  })
                  .passthrough()
              )
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/use-cases/:useCaseId/risk-identifications',
    alias: 'recordRiskIdentification',
    description: `Continuous identification trigger — use expansion, data change, or decision-driver drift.
`,
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: recordRiskIdentification_Body,
      },
      {
        name: 'useCaseId',
        type: 'Path',
        schema: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            riskIdentificationId: z
              .string()
              .regex(/^rid_[0-9A-HJKMNP-TV-Z]{26}$/),
            useCaseId: z.string().regex(/^ucs_[0-9A-HJKMNP-TV-Z]{26}$/),
            trigger: z.enum([
              'use_expansion',
              'data_change',
              'driver_drift',
              'regulatory_change',
              'periodic',
            ]),
            description: z.string().optional(),
            reassessmentRequired: z.boolean().optional(),
            identifiedAt: z.string().datetime({ offset: true }),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
